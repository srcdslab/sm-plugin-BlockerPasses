#if defined _blockerpasses_included
	#endinput
#endif
#define _admin_included

/**
 *	@return true if locked otherwise false
 *	@return true если заблокировано, иначе false
*/

native sBlockerpasses_IsLocked();